spring:
  cloud:
    nacos:
      discovery:
        server-addr: http://${ip}:8848
#      config:
#        server-addr: http://localhost:8848
#        file-extension: yaml
logging:
  level:
    root: debug